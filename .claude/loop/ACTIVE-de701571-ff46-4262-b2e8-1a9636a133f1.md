# LOOP ACTIVE — ZINOSB → train-dia 8축 시스템 이식

세션: `de701571-ff46-4262-b2e8-1a9636a133f1` · 시작 2026-08-09
원본: `C:\Users\smrt2\Documents\GitHub\zinosb-marketplace` (2026-08-09 clone)

> 이 파일만 읽으면 대화가 압축돼도 이어갈 수 있어야 한다.
> 매 축 종료 시 갱신: 목표 / 확정 결정 / 검증 명령과 **실제 출력** / 발견 문제 / 다음 행동

---

## 기준선 (PHASE A, 2026-08-09)

| 항목 | 값 |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm test` | Test Files 3 passed · **Tests 54 passed** |
| 스캔 우주 | CSS 79 · TSX 145 · TS 141 = **365파일** |
| 정의된 CSS 커스텀 프로퍼티 | 684종 |

### 8축 실측 원본 수치

| 축 | 실측 |
|---|---|
| 1 safe-area | raw `env()` **179** : `var(--sat)` 6 · `--sab`/`--sal`/`--sar` 전역 정의 없음 · 플랫폼 분기 전무 |
| 1 뷰포트 | bare `100vh` 31 (폴백 짝 26 정상 / **단독 5 결함**) · `100dvh` 28 · `100svh` 0 |
| 6 토큰 | 팬텀 18종 → 검증 결과 **진짜 죽은 선언 11종** / 인라인 주입 7종(정상) |
| 7 z-index | raw ≥10 **16종** (10·11·20·50·60·100·150·200·1000·1100·1150·1200·1300·1305·1310·9999) · `--dia-layer-*` 토큰은 **이미 존재**(미사용) |
| 7 타이포 | font-size 54종 · raw px **29종/98건** (최소 10px = 하한 위반) |
| 8 인터랙션 | duration **52종/364건** · easing 13종 · 모션 토큰 **없음** |
| 5 패딩 | 컨테이너 가로 패딩 **17종** · `--dia-page-pad` 없음 · 블리드 유틸 없음 |
| 4 헤더 | 헤더성 블록 **192개 / 13 feature** · 공유 컴포넌트 **0개** · 제목 9종 · 행높이 7종 · 패딩 28종 · sticky 9/192 · safe-area 32/192 · 백/닫기 심볼 429건/83파일 |
| 3 햅틱 | onClick 703 · `<button>` 647 · 햅틱 13건/4파일 = **3.5%** · SSOT 없음 |
| 9 알림 | Toast 호출 34건/9파일 · 전역 store **없음** · `alert()` **0건**(이미 준수) |
| 10 백로직 | API **39개**(지시서 추정 4개) · raw `NextResponse.json` **340건** · Zod 0 · zod 미설치 · 봉투 SSOT 없음 |

### 지시서 대비 정정 3건
1. API는 4개가 아니라 **39개**
2. 헤더는 "HomeHeader 하나뿐"이 아니라 **공유 컴포넌트 0개** (13 feature 전부 자체 구현)
3. `--dia-layer-*` z-index 토큰은 **이미 존재** — 신설이 아니라 미완주 마이그레이션 문제

---

## 확정된 결정 (되돌리지 말 것)

| # | 결정 | 근거 |
|---|---|---|
| D1 | `prefers-reduced-motion` **존중 유지** — ZINOSB 전면 미존중 정책 이식 안 함 | 50-60대 + 야간근무 + train-dia CLAUDE.md §4.7이 이미 "대응 필수". ZINOSB 정책은 라이딩 앱 몰입감 전용 |
| D2 | 햅틱은 **독립 축에서 제거** → 축6(Toast store)에 흡수. 개별 버튼 703곳 배선은 다음 세션 | ZINOSB 방식대로 토스트/확인시트에서 자동 발화하면 배선 0으로 커버. iOS Safari `navigator.vibrate` 미지원이라 개별 배선 ROI 낮음 |
| D3 | 백로직은 **일괄 변환 금지**, baseline 래칫 + 신규/수정분부터 | 봉투를 씌우는 순간 그 API를 읽는 프론트 소비처가 조용히 빈다. 라이브 175명 |
| D4 | 전역 `--sab` 에 최소 높이 바닥값 **깔지 않음** | 하단 inset 이 정상 0 인 기기에서 없던 여백이 생긴다. ZINOSB 도 같은 이유로 전역 미변경(네이티브 주입으로 따로 해결했으나 train-dia 는 PWA 라 그 경로 없음) |
| D5 | 플랫폼 클래스는 **standalone 일 때만** 부착 | 일반 Safari 브라우징은 `env()` 가 정상 0 → iOS 바닥값 20px 를 걸면 없던 여백이 상단에 생김 |
| D6 | `--z-*` prefix 신규 도입 금지 — train-dia 는 `--dia-*` | 지시서 원칙. 단 이미 들어온 `--zg-*`(페이크글라스)·`--z-seg-*`·`--z-3d-*` 는 존중 |
| D7 | 새 토큰/유틸/컴포넌트 신설 전 **기존 존재 여부 확인 의무** | 실제로 `.safeTop`/`.safeBottom` 을 중복 신설했다가 걷어냄(globals.css 에 이미 있었음). UI-ADOPT-001 |

---

## 축 진행 상황

### ✅ 축1 — safe-area / 뷰포트 (커밋 `bc9045a`, push 완료)

**목표**: 노치·다이내믹 아일랜드 침범 차단. `viewport-fit:cover` + `black-translucent` 인데 방어가 없던 P0.

**한 일**
1. `tokens.css` — `--sat/--sab/--sal/--sar` SSOT 4종 정의
2. `globals.css` — `html.pwa-ios` = `max(env(...,20px),20px)` / `html.pwa-android` = `env()` 신뢰 / `@media (display-mode:standalone)` 1차 방어
3. `layout.tsx` — hydration 전 플랫폼 감지 인라인 스크립트 (standalone 한정)
4. 소비처 이관 **171건 / 54파일** (raw `env()` → `var(--sa*)`, 정의부는 raw 유지)
5. 단독 `100vh` 5건 → `100dvh` 폴백 짝 **보강**(치환 아님)
6. `scripts/check-safearea.mjs` 신설 — CSS-SAFEAREA-001 / CSS-VIEWPORT-UNIT-001 전량 스캔
7. `registry.json` 1.1.0→1.2.0 · `design-system.md` 룰 본문 2개

**검증 (실제 출력)**
```
npm run typecheck            → exit 0
npm test                     → Test Files 3 passed (3) / Tests 54 passed (54)
npm run check:quality-gates  → 8종 전부 ✅, exit 0
  ✅ check:safearea — CSS-SAFEAREA-001 / CSS-VIEWPORT-UNIT-001 위반 0건 (스캔 365파일)
npm run build                → 성공 (전 라우트 빌드)
git push origin main         → 409340a..bc9045a  main -> main
```

**가드 의무 검증 (위반 주입 → FAIL → 원복)**
```
주입 후: ❌ CSS-SAFEAREA-001 2건 + CSS-VIEWPORT-UNIT-001 1건, exit=1
        SAFEAREA-EXCEPTION 주석 경로 통과 ✅ / 100vh+100dvh 폴백 짝 통과 ✅
원복 후: ✅ 위반 0건, exit=0
```

**발견한 문제 (기록)**
- 가드가 자기 문서의 **주석 산문**을 위반으로 잡았다 → `stripComments()` 추가. 룰을 문서화할수록 위반이 느는 자기모순이었음.
- 기존 가드들의 `walk()` 가 `.module.css`/`globals.css` 만 스캔 = **스캔 우주 사각지대**. `tokens.css`·`premium-fake-glass.css`·`apex-globals.css` 가 렌즈 밖. 신규 가드는 전체 css+tsx+ts 스캔으로 작성.
- `.safeTop`/`.safeBottom` 유틸을 중복 신설했다가 걷어냄 (globals.css 170~178줄에 이미 존재).

**실폰 QA 항목** → `docs/QA_CHECKLIST.md` (축 전체 종료 시 산출)

---

### ✅ 축2 — 팬텀 토큰 + `check:token-refs` (커밋 `93c4e65`, push 완료)

**자기정정**: PHASE A 의 "죽은 토큰 11건"은 **오측정**이었다. 탐지기 결함 2개 —
(1) 정의 탐지를 줄머리로 한정해 `.cls { --x: v; }` 한 줄 규칙을 놓침(거짓 양성 다수),
(2) 폴백 유무를 구분 안 함(`var(--x, 폴백)` 은 조용히 죽지 않는다).
**정확한 값: FAIL 3종 / WARN 3종.**

**실버그 2건**
- `--z-3d-lime-hi/lo/shadow` → apex `.canvasRetryBtn` 의 `background`+`box-shadow` 가 통째로 죽어
  어두운 배경(#0a0d1e) 위 어두운 글자(#101408)만 남아 **버튼이 안 보였다**. `--dia-lime` 팔레트로 계열 완성.
- `--dia-sky-dim` 누락 → 팔레트에서 sky 만 `-dim` 이 없어 영상가이드 sky 그룹만 회색 폴백.

**추가**: `--dpr` 을 layout.tsx 에서 주입 → `--z-glass-hairline: round(1px, calc(1px / var(--dpr,1)))` 이
폴백 1 로만 돌던 것을 실제 기기 픽셀비로 계산. 로컬 실측 DPR 3 반영 확인.

**검증**: token-refs FAIL 0/WARN 0 (365파일·704종) · typecheck 0 · test 54/54 · gates 9종 ✅ · build 9.6s ·
push `bc9045a..93c4e65`. 가드 의무검증(FAIL/WARN/예외/같은줄정의/원복) 전부 통과.

---

### ✅ 축3 — z-index 사다리 완주 (`--dia-layer-*`)

**실측**: raw z-index 24종 135건. 레이어급(≥10) **16종 45건**. `--dia-layer-*` 토큰은
tokens.css 에 정의만 있고 **사용 0회**. 값이 10·11·20·50·60·100·150·200·1000·1100·1150·1200·1300·1305·1310·9999
로 흩어져 기존 사다리(최대 200 + critical)로는 표현 불가.

**핵심 결정 (D8)** — **번호를 재배치하지 않는다.** 라이브 175명 앱에서 스택 순서를 바꾸면
모달이 토스트 뒤로 숨는 사고가 난다. 대신 **현재 값을 그대로 가진 이름을 붙여 치환**했다
(값 보존 = 계산 결과 동일 = 회귀 구조적 불가). 사다리를 예쁘게 정리하는 건 이름이 붙은 뒤의 다음 문제.

- tokens.css 사다리 8단 → **18단** 확장(sticky-above 11 / overlay-above 60 / modal-above 150 /
  fullscreen 1000·1100 / viewer 1150·1200·1300·1305·1310 신설, 전부 현재 사용값)
- 치환 **45건 / 24파일**, 매핑 없음 0건. 로컬 스택(-1~9) 90건은 대상 제외.
- 축1 이관 아티팩트 `var(--sat, var(--sat))` **11건 정리**(apex 2파일)
- `scripts/check-zlayer.mjs` 신설 — 레이어급 raw 숫자 FAIL, 로컬 스택 통과, ZLAYER-EXCEPTION 예외

**검증**: typecheck 0 · test 54/54 · gates **10종** ✅ · 로컬 런타임에서 사다리 18단 전부 해석 +
AuthGate z-index 9999 정상. 가드 의무검증: 500→FAIL / 5→통과 / EXCEPTION→통과 / 원복→0건.

---

### ✅ 축4 — 페이지 게터 SSOT + 블리드 (`--dia-page-pad`)

**구조 실측**: AppShell `.content` 는 가로 게터를 **안 잡는다**(`--sal/--sar` 만). 탭마다 `.container`
가 자기 게터(`var(--dia-space-4)`=16px)를 잡고, 헤더가 `margin-inline: calc(-1 * var(--dia-space-4))`
로 상쇄해 full-width 로 빠져나갔다가 안쪽 패딩으로 되돌린다(블리드 4곳). **지금은 일치하지만
강제하는 게 없었다** — ZINOSB 가 "게터 16 vs 복귀 28 이 3개월 생존"으로 겪은 바로 그 구조.

- `--dia-page-pad: var(--dia-space-4)` SSOT 신설(값 보존 16px)
- 탭 컨테이너 7개(Calendar/Exchange/Compare/Contacts/Duty/Line5/More) 가로칸 → 게터 토큰.
  `padding: var(--dia-space-4)` 1값 → `var(--dia-space-4) var(--dia-page-pad)` (세로는 유지)
- 블리드 4곳의 **상쇄 + 복귀** 둘 다 게터 토큰으로. 총 15곳 이관, 잔존 0.
- 유틸 신설: `.diaBleed` / `.diaBleedInset` / `.diaScrollRow` (상쇄를 손으로 안 적게)
- `scripts/check-bleed.mjs` — **UI-BLEED-001(탈출) + UI-BLEED-002(복귀)**

**가드 설계 함정 2건 (초안에서 실제 발생)**
1. "음수 가로 마진"을 전부 블리드로 보면 `-1px`(srOnly)·`-8px`(아이콘 정렬)·`-150px`(센터링)이
   잡혀 **거짓 FAIL 8건**. → 판정을 **좌우 대칭 + 게터 크기(16px) 이상**으로 좁힘.
2. 규칙 블록 줄번호가 전부 `:1` 로 나옴 — `m.index` 가 앞 규칙 `}` 직후를 가리켜서.
   → 셀렉터 첫 비공백 위치로 보정.

**검증**: typecheck 0 · test 54/54 · gates **11종** ✅ ·
로컬 실측(360x800x3): `--dia-page-pad=16px === --dia-space-4`,
Line5/More 에서 **탈출 -16px / 복귀 +16px 정확히 일치**, 3개 탭 전부 overflow 0.
가드 의무검증: `--dia-space-4` 상쇄→001 FAIL / 복귀 28px→**002 FAIL(ZINOSB 사고 케이스 재현 검출)** /
정상→통과 / `-8px`·`-150px`→통과(거짓양성 없음) / EXCEPTION→통과 / 원복→0건.

**운영 메모**: CSS 30여 개를 연속 수정하면 next dev 의 HMR 상태가 꼬여 앱이 로딩 스피너에서
멈춘다(실제 발생). `.next` 삭제 + dev 재시작으로 복구되며 **코드 문제가 아니다** — 로컬 QA 중
빈 화면이 나오면 이걸 먼저 의심할 것.

---

### ✅ 축5 — 백로직 응답 봉투 SSOT + zod (baseline 래칫)

**실측**: API 39개 / raw `NextResponse.json` 340건. 에러 키 분포에서 **`code` 가 123건**으로
이미 지배적이었고 성공만 `{success:true}` 16 / `{ok:true}` 11 / 생 데이터로 갈렸다.

**핵심 결정 (D9)** — **새 모양을 발명하지 않는다.** `errorResponse` 가 기존 123건과
**바이트 단위로 같은 모양**(`{code, message}`)을 내므로 전환해도 응답 JSON 이 1글자도 안 바뀐다.
성공 모양은 **바꾸지 않는다** — 봉투를 씌우면 그 API 를 읽는 화면이 조용히 빈다.
통일의 가치는 "새 모양"이 아니라 에러코드·로깅·검증의 **단일 경로**에 있다.

- `zod@4.4.3` 설치 — **package.json + package-lock.json 양쪽 기록 확인**(진호 경고: 과거 lock 미커밋으로 프로덕션 20분 장애)
- `src/lib/api/response.ts` — `errorResponse`/`okJson`/`internalError`/`parseBody`/`parseQuery` + `ERROR_CODES` 12종 + 코드별 기본 status
- 1호 전환 `/api/edu/level-records` (소비처 2곳: QuizSystem POST 본문 미사용 / LevelRecordsOverlay GET 배열)
- `docs/ops/api-envelope-baseline.json` — 38파일 / raw **323건** 등록
- `scripts/check-response-envelope.mjs` — 증가·신규파일·**stale** 3경로 FAIL

**전환하며 함께 고친 실제 결함 3건**
1. `message: error.message` 로 **Supabase 에러 원문(테이블/컬럼/제약조건)을 클라이언트로 유출** → `internalError` 마스킹
2. `await req.json()` 무방비 → 잘못된 JSON 에 500 스택 노출 → `parseBody`
3. 수동 필드 검증 → zod 스키마

**실호출 검증 (localhost:3004, 관리자 세션)**
```
GET  /api/edu/level-records            → 200 + 배열 (실제 기록 반환, 모양 그대로)
POST 잘못된 JSON                        → 400 {code:'BAD_REQUEST', message:'요청 형식이 올바르지 않아요'}   (이전: 500 스택)
POST {levelId:1} (필드 누락)            → 400 {code:'BAD_REQUEST', message:'levelName을(를) 확인해주세요'}
```

**가드 의무검증**: 증가(1→2)→FAIL · 신규 라우트→FAIL · 부채 갚고 baseline 미갱신→**stale FAIL** · 원복→0건.

**검증**: typecheck 0 · test 54/54 · gates **12종** ✅

---

### ✅ 축6 — Toast 전역 store SSOT + 햅틱 흡수

**진범**: 기존 Toast 는 모듈 레벨 가변 싱글턴(`let addToastFn = null`)이었다. 컨테이너가 마운트될 때
자기 `setState` 를 꽂고 언마운트 때 `null` 로 되돌린다 → **컨테이너가 뜨기 전이나 재마운트 사이에
부른 토스트는 조용히 사라진다**(초기 로드 중 에러 알림이 증발하던 경로).

- `src/stores/toast.ts` — zustand store. 중복 억제 / 최대 3개 / 3초 자동 소멸 / 타이머 누수 정리
- `src/lib/haptic.ts` — **새로 만들지 않고** `useGameFeedback.ts` 의 feature-detect 방식을 라이브러리로 승격(UI-ADOPT-001).
  `canVibrate()` 로 먼저 묻고 미지원이면 아예 안 부른다(try/catch 로 삼키는 것과 다름).
  `prefers-reduced-motion` 존중(D1 일관).
- **햅틱 자동 발화** — store 의 `show()` 가 success/error/warning 에 물린다.
  개별 버튼 703곳 배선 없이 알림 경로 커버(실측 커버리지 3.5% → 알림 100%). 호출부 중복 발화 금지.
- 기존 호출부 **7파일 / 18건 수정 0** — `showToast` 를 같은 경로에서 재수출해 시그니처 유지
- 회귀 테스트 `src/stores/__tests__/toast-store.test.ts` **9건 신설**

**검증**: typecheck 0 · test **63/63**(기존 54 + 신규 9) · gates 12종 ✅

**미검증 항목(실폰 QA 로 이관)**: 브라우저에서 토스트 육안 확인은 실패했다 —
트리거 경로(클립보드 복사·폼 제출)가 CDP 자동화에서 권한/제스처에 막힌다.
store 는 유닛 9건으로 검증됐고 렌더는 6줄 map + typecheck 통과.
→ QA 체크리스트에 "PIN 변경 / 대기충당 등록 시 토스트가 뜨고 진동이 한 번만 울리는지" 항목 추가.

**운영 함정 (중요)**: dev 서버가 도는 중에 `npm run build` 를 돌리면 같은 `.next` 를 서로 덮어써
`Cannot find module './5611.js'` 로 앱이 500 을 낸다. **코드 문제가 아니다.**
축4·축6 에서 두 번 겪었다. → **build 전에 dev 를 먼저 끈다.**

---

### ✅ 축7 — 모션 토큰 (duration/easing)

**또 하나의 미완주 마이그레이션**: `--dia-transition-*` 3종(150/250/350ms)이 **사용 0회**였다.
정의만 있고 실사용 지배값(0.12s 97건 · 0.15s 53건)과 아예 안 맞아서 아무도 안 썼다.

**핵심 결정 (D10)** — 토큰을 "예쁜 숫자"로 잡지 않고 **실사용 지배값 그대로** 잡았다.
값 보존이라 이관해도 모션이 1ms 도 안 달라진다. 0.1/0.12/0.15 는 CLAUDE.md §2.6
press 3티어(아이콘/버튼/카드)와도 일치한다. **토큰을 실사용에 맞춘 것**이지 그 반대가 아니다.

- duration 5단계 · easing 5종 신설. 레거시 `--dia-transition-*` 는 팬텀 방지를 위해 별칭으로 유지
- 이관 **334건 → 잔여 27건 (92% 감소)**, 나머지 baseline
- `scripts/check-motion.mjs` — baseline 래칫(증가·신규파일·stale 3경로 FAIL)

**이관 함정**: 1차 스크립트가 줄 단위라 **멀티라인 `transition` 선언을 통째로 놓쳤다**
(globals.css 의 3줄짜리 button transition 이 그 예). 2차로 선언 단위 정규식을 돌려 11건 추가 처리.
→ CSS 마이그레이션은 줄이 아니라 **선언 단위**로 볼 것.

**검증**: typecheck 0 · test 63/63 · gates **13종** ✅ ·
가드 의무검증: raw duration 추가→FAIL / 갚고 baseline 미갱신→stale FAIL / 원복→0건.

---

### ⏭ 다음: 축8 — 헤더 SSOT (192블록/13feature, 최대 체감·최대 리스크) + 360px 줄바꿈 결함 3건

죽은 선언 목록:
`--jw` · `--vg-accent` · `--vg-accent-bg` · `--dia-sky-dim` · `--vreg-accent` ·
`--z-3d-lime-hi` · `--z-3d-lime-lo` · `--z-3d-lime-shadow` · `--_dabCard` · `--_dabIcon` · `--_dabInk` · `--dpr`

- `--z-3d-lime-*` 3종 = apex 게임이 zinosb 라임 토큰 참조 → **이식 누락 흔적**.
  진호 지시: zinosb 값 그대로 가져오지 말고 **train-dia 팔레트 `--dia-lime`(이미 존재)로 대체**. `--z-*` prefix 신규 도입 금지.
- 인라인 주입 7종(`--bn-n`/`--bn-i`/`--jw`?/`--reg-font-size`/`--pct`/`--chip-color`/`--pc`)은 정상이므로 가드에서 예외 처리 필요 —
  JS `setProperty` + JSX `style={{'--x':...}}` 양쪽을 정의로 인정해야 거짓 FAIL 이 안 난다.

---

## 남은 큐 (확정 순서)

1. ~~safe-area + 뷰포트~~ ✅ `bc9045a`
2. 죽은 토큰 + `check:token-refs`
3. z-index 사다리 완주 (raw 16종 → `--dia-layer-*`)
4. 페이지 게터 SSOT `--dia-page-pad` + 블리드 유틸 + `check:bleed`
5. 백로직 응답 봉투 SSOT + zod (baseline 래칫·점진)
6. Toast 전역 store SSOT **+ 햅틱 SSOT 흡수**
7. 모션 토큰 (duration 52종 → 5단계)
8. 헤더 SSOT (192블록/13feature — 최대 체감·최대 리스크)

### 진호 추가 요청 큐 (8축 이후 / 관련 축에 흡수)
- Android/iOS **뒤로가기 로직** 통일 · iOS **스와이프 백** 제스처 → 축8(헤더/백버튼 SSOT)에 흡수
- **바텀시트 full 높이**가 노치/다이내믹 아일랜드를 안 가리도록 클램프 → 축1 토큰 활용, 축8 전후로 처리
- 공지/게시판 **실시간(ms) 반영** — Supabase realtime 검토
- **Vercel 배포 후 SW 캐시 갱신** — 앱 재시작 시 새 버전 적용 (iOS/Android 공통)
- **사용자 데이터 저장 회귀 검증** (localStorage / Supabase / storage)

---

## 라이브 서비스 안전장치 (매 push 마다)

- `src/data/*` **수정 금지** (cycle/schedules/holidays/line5/contacts/tips)
- `src/lib/schedule` 계산 로직 **범위 밖**
- push 후 스모크: 홈 / 캘린더 / 비교 화면 에러 없이 뜨는지
- 깨지면 **되돌리기 우선**, 원인 분석은 그다음
- 한 커밋 = 한 축
